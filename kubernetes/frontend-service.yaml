apiVersion: v1
kind: Service
metadata:
  name: task-manager-frontend-service
spec:
  type: NodePort
  selector:
    app: task-manager-frontend
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30000