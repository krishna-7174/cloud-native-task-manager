apiVersion: v1
kind: Service
metadata:
  name: task-manager-backend-service
spec:
  type: NodePort
  selector:
    app: task-manager-backend
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30001